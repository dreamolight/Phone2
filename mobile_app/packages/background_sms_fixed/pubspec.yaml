name: background_sms
description: Fixed background_sms plugin with V2 embedding support.
version: 0.0.5

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.j.background_sms
        pluginClass: BackgroundSmsPlugin
